'use strict';

var mainController = angular.module('mainController', []);

mainController.controller('MainController', ['$scope', 
  function($scope,$http) {
	// Do nothing
	// Maybe i should put here the logic to make a menu 'active'
}]);
